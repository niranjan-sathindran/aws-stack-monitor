# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "aws-stack-monitor"

[setup]
script = ""

[[actions]]
name = "Run"
icon = "run"
command = "/bin/zsh -lc 'repo_root=$(git rev-parse --show-toplevel 2>/dev/null || pwd) && cd \"$repo_root\" && pids=$(lsof -tiTCP:8501 -sTCP:LISTEN -n -P || true) && if [ -n \"$pids\" ]; then kill $pids || true; sleep 1; fi && if command -v streamlit >/dev/null 2>&1; then launcher=\"streamlit\"; elif [ -x \"$HOME/opt/anaconda3/bin/streamlit\" ]; then launcher=\"$HOME/opt/anaconda3/bin/streamlit\"; else echo \"streamlit not found in PATH; activate your environment or install requirements first\" >&2; exit 1; fi && nohup \"$launcher\" run app.py --server.headless true --server.port 8501 >/tmp/aws-stack-monitor.log 2>&1 < /dev/null &'"
